// requiring initApp class from init.js
const initApp = require("./userCLI/init.js");
// setting app as a new initApp() instance
const app = new initApp();

// calling app.init() initialize app
app.init();
